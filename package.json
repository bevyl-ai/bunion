{
  "name": "bunion",
  "version": "0.1.0",
  "description": "A Bun/TS port of OpenAI's Symphony — point it at a repo + Linear project and it ships simple tickets autonomously via Codex.",
  "type": "module",
  "bin": {
    "bunion": "src/cli.ts",
    "bunion-runner": "src/runner-cli.ts"
  },
  "engines": { "bun": ">=1.3" },
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "start": "bun src/cli.ts start"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.7.2"
  }
}
